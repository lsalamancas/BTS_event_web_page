{
  "name": "simposium-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "deploy": "touch dist/.nojekyll && gh-pages -d dist --dotfiles",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.10.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
